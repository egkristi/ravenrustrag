{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ravenrustrag.serviceAccountName" . }}
  labels:
    {{- include "ravenrustrag.labels" . | nindent 4 }}
{{- end }}
