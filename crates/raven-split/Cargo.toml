[package]
name = "raven-split"
version.workspace = true
edition.workspace = true

[dependencies]
raven-core = { path = "../raven-core" }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
